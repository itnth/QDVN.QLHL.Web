import { defineComponent, onMounted, ref } from "vue";
import "./Managers.css"
export default defineComponent({
    components: {
    },
    setup(props, ctx) {
        onMounted(() => {
            // ...
        })

        return {
        }
    },
})