import { defineComponent, onMounted, ref } from "vue";
import "./Student.css"
export default defineComponent({
    components: {
    },
    setup(props, ctx) {
        onMounted(() => {
            // ...
        })

        return {
        }
    },
})