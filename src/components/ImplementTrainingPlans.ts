import { defineComponent, onMounted, ref } from "vue";
import "./ArmyUnit.css"
export default defineComponent({
    components: {
    },
    setup(props, ctx) {
        onMounted(() => {
            // ...
        })

        return {
        }
    },
})