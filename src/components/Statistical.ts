import { defineComponent, onMounted, ref } from "vue";
import "./Statistical.css"
export default defineComponent({
    components: {
    },
    setup(props, ctx) {
        onMounted(() => {
            // ...
        })

        return {
        }
    },
})