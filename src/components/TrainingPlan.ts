import { defineComponent, onMounted, ref } from "vue";
import "./TrainingPlan.css"
export default defineComponent({
    components: {
    },
    setup(props, ctx) {
        onMounted(() => {
            // ...
        })

        return {
        }
    },
})